server:
  port: 8889

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/javierGomez18/infrastructure-platform
          default-label: develop
          search-paths: config-server
          clone-on-start: true