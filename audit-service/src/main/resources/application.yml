spring:
  application:
    name: audit-service
  profiles:
    active: dev
  config:
    import: "configserver:"
  cloud:
    config:
      uri: http://localhost:8889
      profile: dev
      label: develop
